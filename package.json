{
    "name": "front-end-test",
    "private": true,
    "version": "0.0.0",
    "description": "Teste de front-end-test",
    "repository": "https://github.com/ajmasters/front-end-test",
    "license": "MIT",
    "devDependencies": {
        "bower": "^1.7.7",
        "http-server": "^0.9.0"
    },
    "scripts": {
        "postinstall": "bower install",
        "update-deps": "npm update",
        "postupdate-deps": "bower update",
        "prestart": "npm install",
        "start": "http-server -a localhost -p 5000 -c-1 ./",
        "pretest": "npm install",
        "preupdate-webdriver": "npm install",
        "update-webdriver": "webdriver-manager update"
    }
}
